node_modules
dist
.env
*.log
.DS_Store

# Local Claude Code state — track committed skills, hooks, and settings; ignore
# machine-local state (worktrees, per-path memory, settings.local.json).
/.claude/*
!/.claude/skills/
!/.claude/hooks/
!/.claude/settings.json
